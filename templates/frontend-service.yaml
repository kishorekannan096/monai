apiVersion: v1
kind: Service
metadata:
  name: monai-frontend
  labels:
    app: monai-frontend
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: monai-frontend
