apiVersion: v1
kind: Service
metadata:
  name: monai-backend
  labels:
    app: monai-backend
spec:
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: monai-backend
